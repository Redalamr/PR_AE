# ============================================
# Projet Majeur IA — Pipeline OCR Tableau Blanc
# ESAIP · IR4-S8 · 2025–2026
# V2 — Streamlit + LLM + LaTeX-OCR
# ============================================

# --- Core ---
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# --- ML / Deep Learning ---
torch>=2.0.0
torchvision>=0.15.0

# --- OCR Engines ---
python-doctr[torch]>=0.7.0
pytesseract>=0.3.10
transformers>=4.30.0       # TrOCR + Nougat

# --- V2 : LaTeX-OCR ---
pix2tex>=0.1.1             # LaTeX-OCR (formules → LaTeX)
# nougat-ocr               # Optionnel — Nougat (Meta), très lourd (~2GB)

# --- V2 : Correction LLM ---
openai>=1.0.0              # OpenAI API (GPT-4o-mini)
anthropic>=0.25.0          # Anthropic API (Claude 3.5 Sonnet)

# --- V2 : Interface Web ---
streamlit>=1.30.0

# --- Data Augmentation ---
albumentations>=1.3.0

# --- Evaluation ---
jiwer>=3.0.0               # CER / WER
scikit-learn>=1.3.0

# --- Experiment Tracking ---
mlflow>=2.5.0

# --- PDF Generation ---
reportlab>=4.0.0

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Utilities ---
tqdm>=4.65.0
pyyaml>=6.0
